/** Hot Reload Command */
export const HOT_CMD = "/reload";

/** Hostname */
export const HOSTNAME = "127.0.0.1";

/** Port */
export const PORT = 8080;

/** Default configuration */
export default {
  hostname: "0.0.0.0",
  port: 8080,
  watch: true,
  reloadCommand: "/hot",
  publicDir: "src"
};